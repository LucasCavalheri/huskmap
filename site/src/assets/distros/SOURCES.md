# Distro marks

From [Simple Icons](https://simpleicons.org) (CC0 1.0), via `@iconify-json/simple-icons`
1.2.96. Shapes unchanged; painted white so the page can tint them. The marks belong to their
projects and only say which package fits which distro.

debian, ubuntu, linuxmint, fedora, opensuse, redhat, archlinux, manjaro, alpinelinux,
gentoo, voidlinux, nixos, linux
